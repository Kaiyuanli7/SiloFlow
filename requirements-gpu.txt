# GPU Acceleration Requirements for SiloFlow
# Install these for maximum performance with GPU-accelerated data processing

# Basic GPU support (for multi-GPU detection and training)
pynvml>=11.4.1

# RAPIDS cuDF for GPU-accelerated data processing (10-150x speedup)
# Choose ONE of the following based on your CUDA version:

# Option 1: OpenCL support (for AMD/Intel GPUs)
pyopencl>=2023.1

# Option 1: CUDA 11.8 (most common)
cudf-cu11

# Option 2: CUDA 12.x (latest)
# cudf-cu12

